# 資料庫考題練習指南 (Step-by-Step)

這份指南會帶你從零開始，使用我為你準備的工具來練習資料庫考題。

---

## 第一部分：觀念釐清

### Q1: 什麼是「題目 ID (Question ID)」？

**A:** 這是**你自己取的名字**。
因為考題都在 PDF 裡，系統不知道你想練習哪一題。所以當你想練習「112年關務特考第3題」時，你可以自己幫它取一個 ID，例如：`112_guanwu_q3` 或簡單的 `q1`。

### Q2: 整個流程是什麼？

1. **選題**：打開 PDF，看中一題 SQL 題目。
2. **建檔**：告訴系統「我要練習這題」（給它一個 ID）。
3. **填資料**：系統會給你一個 SQL 樣板，你要把題目給的資料（表格欄位、數據）填進去。
4. **載入**：系統把這些資料塞進 MySQL 資料庫。
5. **解題**：打開 DBeaver，連線到資料庫，開始寫 SQL 解題。

---

## 第二部分：詳細操作流程

### 步驟 1：建立練習檔案

打開終端機 (Terminal)，進入專案目錄，輸入以下指令來建立一個新題目（假設我們叫它 `practice_01`）：

```bash
python3 db_practice_manager.py create practice_01
```

系統會告訴你：`Created template at datasets/practice_01.sql`。
這表示它幫你產生了一個 SQL 檔案。

### 步驟 2：輸入題目資料

現在，請用你喜歡的編輯器（例如 VS Code）打開剛剛產生的檔案：
`datasets/practice_01.sql`

你會看到類似這樣的內容：

```sql
CREATE DATABASE IF NOT EXISTS practice_practice_01;
USE practice_practice_01;

-- Define Tables
-- CREATE TABLE ...

-- Insert Data
-- INSERT INTO ...
```

**你的任務**：
根據考題的描述，把 `CREATE TABLE` 和 `INSERT INTO` 寫進去。
*如果不確定怎麼寫，你可以把考題貼給我，我幫你寫這部分！*

### 步驟 3：載入資料到資料庫

存檔後，回到終端機，告訴系統把這份資料「吃」進去：

```bash
python3 db_practice_manager.py load practice_01
```

如果成功，會顯示 `Successfully loaded practice_01!`。

### 步驟 4：設定 DBeaver (初次使用)

1. 打開 **DBeaver**。
2. 點擊左上角的插頭圖示 (New Database Connection)。
3. 選擇 **MySQL**，按 Next。
4. **Server Host**: `localhost` (或 `127.0.0.1`)
5. **Port**: `3306`
6. **Username**: `root`
7. **Password**: (留空，或者試試 `root`，依你的安裝而定)
8. 點擊 **Finish**。

### 步驟 5：開始解題

1. 在 DBeaver 左側的 **Database Navigator**，展開剛剛建立的連線。
2. 你會看到一個資料庫叫做 `practice_practice_01` (這是系統自動加的前綴)。
3. 右鍵點擊該資料庫 -> **SQL Editor** -> **Open SQL Script**。
4. 現在你可以開始寫題目要求的 SQL 了！
    * 例如：`SELECT * FROM Student WHERE score > 80;`
5. 按 `Ctrl + Enter` (或 `Cmd + Enter`) 執行，下方就會顯示結果。

---## 如何使用？

### 🎯 快速開始（推薦）

我們提供了簡化的命令 `dbpractice`，使用超簡單！

### 1. 查看所有可用的題目

```bash
./dbpractice list
```

### 2. 創建一個新題目

```bash
./dbpractice create <題目ID>
```

例如：

```bash
./dbpractice create q114_1
```

這會在 `datasets/` 目錄下創建一個 `q114_1.sql` 檔案。

### 3. 編輯題目檔案

打開 `datasets/q114_1.sql`，填入：
* 題目描述（註解）
* CREATE TABLE 語句
* INSERT INTO 語句

### 4. 載入到資料庫

```bash
./dbpractice load q114_1
```

---

### 🔧 進階用法（如果需要）

如果你不想用 `./dbpractice`，也可以直接使用：

```bash
python3 db_practice_manager.py list
python3 db_practice_manager.py create <題目ID>
python3 db_practice_manager.py load <題目ID>
```

---

## 常見問題

**Q: 我執行 `create` 時報錯？**

* 檢查你有沒有輸入 ID。
  * ❌ `./dbpractice create` (錯誤)
  * ✅ `./dbpractice create my_test` (正確)

**Q: 我怎麼知道有哪些題目 ID？**

* 輸入 `./dbpractice list` 可以看到你目前已經建立過的所有練習。

**Q: 每次都要重新設定 DBeaver 嗎？**

* 不用！連線設定好一次就在那裡了。
* 每次載入新題目 (`load`) 後，只要在 DBeaver 左側列表按 **F5 (Refresh)**，新的資料庫就會出現。
